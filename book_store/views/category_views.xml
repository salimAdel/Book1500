<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Form View -->
    <record id="view_library_category_view_form" model="ir.ui.view">
        <field name="name">library.category.view.form</field>
        <field name="model">library.category</field>
        <field name="arch" type="xml">
            <form string="Category">
                <sheet>
                    <group>
                        <field name="name_ar"/>
                        <field name="name_en"/>
                        <field name="name_ind"/>
                    </group>
                   
                </sheet>
            </form>
        </field>
    </record>

    <!-- List View -->
   <record id="category_view_list" model="ir.ui.view">
            <field name="name">category.view.list</field>
            <field name="model">library.category</field>
            <field name="arch" type="xml">
                <list string="Category View">
                    <field name="name_ar"/>
                    <field name="name_en"/>
                    <field name="name_ind"/>
                    <!-- Add your fields here -->
                </list>
            </field>
    </record>

    <!-- Action -->
    <record id="action_library_category" model="ir.actions.act_window">
        <field name="name">Categories</field>
        <field name="res_model">library.category</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>
